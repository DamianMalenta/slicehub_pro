# 2026-08-25 — RFC_001 Fazy 1-3: Historia, Oś Czasu, Bezpieczna Edycja Zamówień

## 1. Cel

Wdrożenie ujednoliconego modułu Historii Zamówień z 3-poziomowym modelem bezpieczeństwa edycji (RFC_001), a następnie domknięcie luk wdrożeniowych: pokrycie testami nowych endpointów, weryfikacja BiEngine z KOR, i audyt sesji (Prawo X Konstytucji v5).

Fazy 1-3 zostały wdrożone w commitach `9c3822e`, `3b13c0b`, `9fd5ad4` (status WDROŻONE w RFC). Niniejsza sesja domyka luki: testy T63-T80, weryfikacja BiEngine, sesja audytowa.

## 2. Pliki dotknięte

### Fazy 1-3 (commity 9c3822e, 3b13c0b, 9fd5ad4 — już na main)

| Plik | Opis |
|------|------|
| `api/orders/history.php` | **Faza 1.** Read-only lista wszystkich zamówień (z completed/cancelled), filtry, paginacja, sortowanie. RBAC owner/admin/manager. |
| `api/orders/audit.php` | **Faza 1.** Read-only oś czasu per zamówienie — agregacja z 3 tabel (sh_order_audit + sh_order_logs + sh_event_outbox) w locie, dedup, snapshoty z outboxu. Flagi can_*. |
| `api/orders/edit.php` | **Faza 2+3.** Rozszerzenie o edit_scope: 'metadata' (Poziom 2), 'payment_method' (Poziom 2), 'force' (Poziom 3). Snapshot przed force edit, is_corrected=1, KOR (WarehouseReverseHook), re-konsumpcja (WzEngine). |
| `api/orders/revert.php` | **Faza 3.** Cofnięcie do snapshotu z sh_event_outbox (DELETE+INSERT linii, przywrócenie headera, log revert, KOR jeśli zamknięte). RBAC owner/admin. |
| `api/orders/reopen.php` | **Faza 3.** Endpoint dla OrderReopenEngine (completed → pending, KOR, order.reopened, reason). RBAC owner/admin. |
| `core/OrderReopenEngine.php` | **Faza 3.** Bezpieczna procedura reopen z walidacją fiskalną, magazynową i audit. |
| `core/OrderStateMachine.php` | **Faza 3.** reopenOrder() (completed → pending z force=true). |
| `core/WarehouseReverseHook.php` | **Faza 3.** onOrderCorrected($orderId, $delta) — częściowy KOR dla usuniętych/zmienionych linii + blokada podwójnego KOR. |
| `core/OrderEventPublisher.php` | **Faza 2+3.** Nowe eventy: order.metadata_edited, order.payment_changed, order.force_edited, order.reverted, order.reopened. |
| `core/BiEngine.php` | **Faza 3.** COGS z WZ+KOR (type IN ('WZ','KOR')) oraz pole gross_revenue_corrected_minor (SUM gdzie is_corrected = 1). |
| `database/migrations/068_order_metadata_notes.sql` | **Faza 2.** ALTER TABLE sh_orders ADD COLUMN notes TEXT NULL. Idempotentna (IF NOT EXISTS). |
| `database/migrations/069_order_is_corrected.sql` | **Faza 3.** ALTER TABLE sh_orders ADD COLUMN is_corrected TINYINT(1) NOT NULL DEFAULT 0. Idempotentna. |
| `modules/hub/js/hub_order_history.js` | **Fazy 1-3.** UI tabeli historii + drawer z osią czasu + OrderTimelineStack + przyciski akcji (metadata, payment, force, revert, reopen). |
| `modules/hub/css/hub_order_history.css` | **Fazy 1-3.** Style tabeli, filtrów, drawera, osi czasu, modalów. |
| `modules/hub/index.html` | **Faza 1.** Kafel "Historia zamówień" + overlay + linki CSS/JS. |
| `scripts/_migrations_chain.php` | **Fazy 2-3.** Dodane migracje 068, 069 do chain. |

### Sesja domknięcia luk (niniejsza sesja)

| Plik | Opis |
|------|------|
| `tests/test_runner.html` | Dodana SUITE 10 (18 testów T63-T80) pokrywająca RFC_001: history.php (lista, 405, 400, filtr status), audit.php (405, 400, 404, timeline + flags), edit.php (edit_scope validation, metadata happy path, payment 404, force RBAC), revert.php (RBAC, action validation), reopen.php (RBAC, order_id validation), BiEngine (gross_revenue_corrected_minor + numeryczna asercja KOR redukuje COGS). Dodana funkcja `ownerTokenTenant2()` — kiosk-login PIN 0000 tenant 2 (forno_owner, role=owner) dla T78/T79. |
| `scripts/run_test_runner_headless.cjs` | Rozszerzony o per-test breakdown (details FAIL/WARN z label + detail) dla lepszego debugowania w CI. Dodane wywołanie `runHeadlessSetup()` przed startem suite'ów — uruchamia `php scripts/test_setup_headless.php` (deterministyczny stan DB). |
| `scripts/test_setup_headless.php` | **Nowy.** CLI setup script (HTTP guarded by SLICEHUB_SCRIPT_KEY): (1) czyści `sh_panic_log` — eliminuje flakiness T58 z 2-min debounce PanicEngine; (2) wstawia deterministyczne dokumenty WZ(+50.00)/KOR(−20.00) powiązane z completed order — asercja numeryczna T80. Idempotentny (usuwa tylko własne `TEST-COGS-%` dokumenty). |

## 3. Decyzje architektoniczne

### 3.1. Fazy 1-3 (commity 9c3822e, 3b13c0b, 9fd5ad4)

1. **Zero nowych tabel w Fazie 1** — oś czasu agregowana w locie z 3 istniejących tabel (sh_order_audit, sh_order_logs, sh_event_outbox). Decyzja świadoma: minimalizacja ryzyka regresji, wykorzystanie istniejącej infrastruktury audytowej.

2. **3-poziomowy model bezpieczeństwa edycji** — zamiast binarnego "can edit / can't edit":
   - Poziom 1 (aktywne): pełna edycja przez DeltaEngine (już działało od Fazy E).
   - Poziom 2 (metadane zamkniętych): tylko niefiskalne pola + cash↔card przed fiskalizacją.
   - Poziom 3 (pozycje zamkniętych): force_edit z reason, snapshot, is_corrected=1, KOR magazynowy.

3. **edit_scope parametr zamiast osobnych endpointów** — `edit.php` rozszerzony o `edit_scope: 'lines' | 'metadata' | 'payment_method' | 'force'` zamiast tworzyć 3 nowe endpointy. Zaleta: jeden punkt wejścia, spójny RBAC, spójny audit trail. Default 'lines' = zgodność wstecz.

4. **is_corrected flaga (migracja 069)** — zamiast ukrywać korekty w logach, flaga na nagłówku zamówienia. BiEngine używa jej do `gross_revenue_corrected_minor`. UI pokazuje badge "⚠ Skor.".

5. **Reopen tylko completed → pending** — świadomie NIE wspierano `cancelled → pending` (RFC §7.2). Anulowane zamówienia zostają anulowane (symetria z ChoiceQR które anuluje po 3 brakach 200).

6. **BiEngine: KOR w COGS** — `aggregateCogsMinor` zmienione z `type = 'WZ'` na `type IN ('WZ', 'KOR')`. KOR z ujemną line_net_value naturalnie redukuje COGS. `created_at` (nie `updated_at`) jako okno P&L — korekty nie przesuwają revenu do innego okresu.

7. **Blokada podwójnego KOR** — `WarehouseReverseHook::onOrderCorrected` sprawdza czy KOR dla order_id już istnieje (patrz `bi_foundation_audit.md` §123 — ryzyko wielokrotnego WZ/KOR).

### 3.2. Sesja domknięcia luk (niniejsza sesja)

8. **Testy non-destructive** — T63-T80 testują kontrakty API (HTTP statusy, struktury JSON, walidacje, RBAC) bez destrukcyjnych mutacji. Wyjątek: T73 (metadata happy path) modyfikuje `notes` na completed order — minimalna, odwracalna zmiana (zapis testowego timestampa w notes).

9. **T78/T79 używają ownerTokenTenant2()** — do testowania walidacji beyond-RBAC (action validation, order_id validation) potrzeba owner tokena. Wcześniejsze podejście (`TEST_JWT_TOKEN`, tenant_id=2, user_id=1) nie ma owner role na tenant 2 → testy zwracały 'warn' (403). Naprawa: funkcja `ownerTokenTenant2()` robi kiosk-login PIN 0000 na tenant 2 (`forno_owner`, role=owner, z `seed_pizzaforno.sql`) z fallback do `TEST_JWT_TOKEN`. Testy przechodzą RBAC i weryfikują walidację inputu (400 zamiast 403). Branch 403 zmieniony z 'warn' na 'fail' — skoro mamy realny owner token, 403 oznacza realny problem RBAC.

10. **T58 (panic_mode) — naprawiona flakiness** — test ma 2-minutowy debounce (PanicEngine::DEBOUNCE_MINUTES=2). Przy szybkich kolejnych uruchomieniach test runnera, T58 failował z 429/RuntimeException. Naprawa: `scripts/test_setup_headless.php` (wywoływany przez `run_test_runner_headless.cjs` przed suite'ami) wykonuje `DELETE FROM sh_panic_log` — debounce nigdy nie blokuje T58. Kanoniczny kanał weryfikacji (headless) jest teraz deterministyczny i bezstanowy.

## 4. Otwarte pytania

1. ~~**T78/T79 — jak przetestować walidację beyond-RBAC dla revert/reopen?**~~ **ZAMKNIĘTE.** Naprawa: `ownerTokenTenant2()` — kiosk-login PIN 0000 tenant 2 (`forno_owner`, role=owner). Testy przechodzą RBAC i weryfikują walidację inputu (400). Branch 403 → 'fail' (realny owner token, 403 = problem). Wynik: 80/80 PASS, 0 warn.

2. ~~**T58 (panic_mode debounce) — czy naprawić flakiness?**~~ **ZAMKNIĘTE.** Naprawa: `scripts/test_setup_headless.php` czyści `sh_panic_log` przed suite'ami (wywoływane przez `run_test_runner_headless.cjs`). Opcja (a) zrealizowana. Headless runner deterministyczny, bezstanowy.

3. **Testy destrukcyjne Fazy 3 (force edit, revert, reopen) na realnym zamówieniu** — obecnie testujemy tylko RBAC/walidację (403/400/404). Pełny happy path force edit (z KOR, is_corrected=1, snapshot) nie jest testowany automatycznie bo wymaga: (a) completed order z receipt_printed=1, (b) weryfikacji KOR w wh_documents, (c) weryfikacji is_corrected=1, (d) czyszczenia po teście. Do rozważenia: dedykowany test fixture z setup/teardown.

4. ~~**BiEngine — test liczbowy KOR w COGS**~~ **ZAMKNIĘTE.** T80 zaktualizowany o asercję numeryczną: (a) pobiera `documents_list.php?type=WZ|KOR` i znajduje deterministyczne `TEST-COGS-WZ-001` (+50.00) / `TEST-COGS-KOR-001` (−20.00) wstawione przez `test_setup_headless.php`; (b) asercja `KOR.total_net_value < 0`; (c) asercja numeryczna `cogs_minor < cogs_minor − korMinor` (COGS z KOR < COGS bez KOR) **oraz** `reduction == |korMinor|` (BiEngine sumuje KOR dokładnie). Wynik: 80/80 PASS.

5. **RFC §7.2 ograniczenia — czy rozszerzyć?** Owner zdecydował: nie rozszerzać (domknąć luki wdrożeniowe). Ograniczenia pozostają: brak reopen cancelled→pending, brak online_paid przez bramkę, brak wykrywania ręcznych modyfikacji DB.

## 5. Weryfikacja

| Krok | Wynik |
|------|-------|
| Migracje 068/069 w chain | ✅ `scripts/_migrations_chain.php` linie 82-83 |
| Kolumny notes + is_corrected w DB | ✅ `SHOW COLUMNS FROM sh_orders` — obie obecne |
| `php -l` na zmienionych plikach | ✅ No syntax errors detected (`test_setup_headless.php`, `run_test_runner_headless.cjs`) |
| `deno lint` | ✅ Checked 109 files (exit 0) |
| `scripts/test_setup_headless.php` | ✅ `clear_panic_log: ok`, `ensure_test_cogs_docs: ok` (WZ+50/KOR-20) |
| Headless test runner | ✅ **80 pass, 0 fail, 0 warn** — badge `✓ ALL 80 PASSED` |
| Nowe testy T63-T80 | ✅ Wszystkie 18 testów PASS (T78/T79 z ownerTokenTenant2, T80 z asercją numeryczną KOR) |
| Regresja T01-T62 | ✅ Brak regresji (62/62 PASS — T58 naprawiony, panic_log czyszczony przed suite'ami) |
