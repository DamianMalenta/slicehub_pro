# =============================================================================
# SliceHub Pro — .gitignore
# =============================================================================

# -----------------------------------------------------------------------------
# Legacy / archiwa (trzymane offline na dysku, poza repo)
# -----------------------------------------------------------------------------
_KOPALNIA_WIEDZY_LEGACY/
_archive/

# -----------------------------------------------------------------------------
# Środowisko / sekrety
# -----------------------------------------------------------------------------
.env
.env.local
.env.*.local
*.env

# CredentialVault master key (Faza 7.6) — NIGDY w repo; generuj przez
# `php scripts/bootstrap_vault.php`. Utrata = utrata dostępu do wszystkich
# zaszyfrowanych credentials w sh_tenant_integrations / sh_webhook_endpoints.
config/vault_key.txt

# -----------------------------------------------------------------------------
# IDE / edytory
# -----------------------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# -----------------------------------------------------------------------------
# PHP / Composer
# -----------------------------------------------------------------------------
vendor/
composer.phar
composer.lock

# -----------------------------------------------------------------------------
# Logi / cache / tmp
# -----------------------------------------------------------------------------
*.log
logs/
cache/
tmp/
temp/
storage/tmp/

# -----------------------------------------------------------------------------
# Uploady / assety runtime (NIE commitujemy treści wrzucanych przez usera)
# -----------------------------------------------------------------------------
uploads/
public/uploads/
storage/uploads/

# -----------------------------------------------------------------------------
# Bazy / dumpy lokalne (nie wrzucamy dumpów do repo)
# -----------------------------------------------------------------------------
*.sql.bak
*_DUMP_*.sql
*.sqlite
*.sqlite3

# -----------------------------------------------------------------------------
# Node (gdybyśmy kiedyś mieli, choć Konstytucja mówi "zakaz")
# -----------------------------------------------------------------------------
node_modules/
npm-debug.log*

# -----------------------------------------------------------------------------
# Cursor / AI tooling
# -----------------------------------------------------------------------------
.cursor/plans/

# -----------------------------------------------------------------------------
# Skrypty diagnostyczne / one-off (mogą leakować sekrety przy uruchomieniu)
# Każdy scripts/debug_*.php to plik tymczasowy — po naprawie rm i koniec.
# -----------------------------------------------------------------------------
scripts/debug_*.php
scripts/probe_*.php
scripts/_local_*.php

# -----------------------------------------------------------------------------
# Python cache
# -----------------------------------------------------------------------------
__pycache__/
*.pyc

# -----------------------------------------------------------------------------
# Lokalne sekrety / nadpisania configów per-deploy
# (template wersja `tenant_config.php` zostaje w repo — env-driven)
# -----------------------------------------------------------------------------
core/local_secrets.php
core/db_config.local.php
tenant_config.local.php
.env.production
.env.staging
